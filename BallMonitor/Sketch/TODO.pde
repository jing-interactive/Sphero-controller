void mainTodo() {
    //finish everything
}

// single connection
// Idle: if new tuio then change to tuioready
// tuioready: wait for connection, 10 seconds, fail?
// androidready: calibration
// if same coordinate > 2 seconds goto forward 
// 